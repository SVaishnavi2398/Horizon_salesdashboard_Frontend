import { SearchprojectallocationsPipe } from './searchprojectallocations.pipe';

describe('SearchprojectallocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchprojectallocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
