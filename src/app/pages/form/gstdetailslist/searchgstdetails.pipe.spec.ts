import { SearchgstdetailsPipe } from './searchgstdetails.pipe';

describe('SearchgstdetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchgstdetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
