import { SearchdealdetaillistPipe } from './searchdealdetaillist.pipe';

describe('SearchdealdetaillistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchdealdetaillistPipe();
    expect(pipe).toBeTruthy();
  });
});
