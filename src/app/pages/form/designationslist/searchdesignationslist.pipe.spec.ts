import { SearchdesignationslistPipe } from './searchdesignationslist.pipe';

describe('SearchdesignationslistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchdesignationslistPipe();
    expect(pipe).toBeTruthy();
  });
});
