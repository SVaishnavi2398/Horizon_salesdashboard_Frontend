import { SearchteamdetailslistPipe } from './searchteamdetailslist.pipe';

describe('SearchteamdetailslistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchteamdetailslistPipe();
    expect(pipe).toBeTruthy();
  });
});
