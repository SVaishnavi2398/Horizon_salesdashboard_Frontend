import { Searchb2binvoicePipe } from './searchb2binvoice.pipe';

describe('Searchb2binvoicePipe', () => {
  it('create an instance', () => {
    const pipe = new Searchb2binvoicePipe();
    expect(pipe).toBeTruthy();
  });
});
