import { SearchsubregionsPipe } from './searchsubregions.pipe';

describe('SearchsubregionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsubregionsPipe();
    expect(pipe).toBeTruthy();
  });
});
