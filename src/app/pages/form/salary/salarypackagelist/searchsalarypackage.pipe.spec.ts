import { SearchsalarypackagePipe } from './searchsalarypackage.pipe';

describe('SearchsalarypackagePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsalarypackagePipe();
    expect(pipe).toBeTruthy();
  });
});
