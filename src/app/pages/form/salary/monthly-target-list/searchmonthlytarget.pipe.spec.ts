import { SearchmonthlytargetPipe } from './searchmonthlytarget.pipe';

describe('SearchmonthlytargetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchmonthlytargetPipe();
    expect(pipe).toBeTruthy();
  });
});
