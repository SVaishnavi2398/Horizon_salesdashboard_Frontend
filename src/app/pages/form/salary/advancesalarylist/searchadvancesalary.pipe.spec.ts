import { SearchadvancesalaryPipe } from './searchadvancesalary.pipe';

describe('SearchadvancesalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchadvancesalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
