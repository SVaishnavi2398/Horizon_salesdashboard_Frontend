import { SearchmonthlysalaryPipe } from './searchmonthlysalary.pipe';

describe('SearchmonthlysalaryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchmonthlysalaryPipe();
    expect(pipe).toBeTruthy();
  });
});
