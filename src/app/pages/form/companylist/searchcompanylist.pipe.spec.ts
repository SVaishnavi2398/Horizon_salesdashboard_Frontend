import { SearchcompanylistPipe } from './searchcompanylist.pipe';

describe('SearchcompanylistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcompanylistPipe();
    expect(pipe).toBeTruthy();
  });
});
