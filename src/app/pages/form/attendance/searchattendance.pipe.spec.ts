import { SearchattendancePipe } from './searchattendance.pipe';

describe('SearchattendancePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchattendancePipe();
    expect(pipe).toBeTruthy();
  });
});
