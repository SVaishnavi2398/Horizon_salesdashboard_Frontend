import { SearchwalkindealslistPipe } from './searchwalkindealslist.pipe';

describe('SearchwalkindealslistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchwalkindealslistPipe();
    expect(pipe).toBeTruthy();
  });
});
