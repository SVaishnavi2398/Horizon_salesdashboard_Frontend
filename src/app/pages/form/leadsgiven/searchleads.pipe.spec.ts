import { SearchleadsPipe } from './searchleads.pipe';

describe('SearchleadsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchleadsPipe();
    expect(pipe).toBeTruthy();
  });
});
