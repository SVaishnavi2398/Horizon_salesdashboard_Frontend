import { SearchchannelpartnerPipe } from './searchchannelpartner.pipe';

describe('SearchchannelpartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchchannelpartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
