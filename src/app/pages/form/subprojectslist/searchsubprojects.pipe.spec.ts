import { SearchsubprojectsPipe } from './searchsubprojects.pipe';

describe('SearchsubprojectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsubprojectsPipe();
    expect(pipe).toBeTruthy();
  });
});
