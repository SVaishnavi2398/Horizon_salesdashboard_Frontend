import { IncentivedetailsPipe } from './incentivedetails.pipe';

describe('IncentivedetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new IncentivedetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
