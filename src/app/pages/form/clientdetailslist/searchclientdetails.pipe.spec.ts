import { SearchclientdetailsPipe } from './searchclientdetails.pipe';

describe('SearchclientdetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchclientdetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
