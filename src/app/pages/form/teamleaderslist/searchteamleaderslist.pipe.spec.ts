import { SearchteamleaderslistPipe } from './searchteamleaderslist.pipe';

describe('SearchteamleaderslistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchteamleaderslistPipe();
    expect(pipe).toBeTruthy();
  });
});
