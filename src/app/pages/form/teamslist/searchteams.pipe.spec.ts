import { SearchteamsPipe } from './searchteams.pipe';

describe('SearchteamsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchteamsPipe();
    expect(pipe).toBeTruthy();
  });
});
