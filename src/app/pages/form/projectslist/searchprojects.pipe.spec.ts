import { SearchprojectsPipe } from './searchprojects.pipe';

describe('SearchprojectsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchprojectsPipe();
    expect(pipe).toBeTruthy();
  });
});
