import { SearchsalesdetailsPipe } from './searchsalesdetails.pipe';

describe('SearchsalesdetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchsalesdetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
