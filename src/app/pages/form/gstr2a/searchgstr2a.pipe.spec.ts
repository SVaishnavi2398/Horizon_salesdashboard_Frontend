import { Searchgstr2aPipe } from './searchgstr2a.pipe';

describe('Searchgstr3aPipe', () => {
  it('create an instance', () => {
    const pipe = new Searchgstr2aPipe();
    expect(pipe).toBeTruthy();
  });
});
